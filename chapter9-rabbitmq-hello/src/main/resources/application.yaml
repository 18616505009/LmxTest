spring:
  application:
    name: chapter9-rabbitmq-hello
  rabbitmq:
    host: localhost
    addresses: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9000/eureka/